이 파일은 1행은 요일, 2행은 일자, 3행은 그 날의 총근무 인원
A열은 휴무요일, B열은 차번호, C열은 기사명, D행은 만근일수, E,F행은 감차일수, G은 실근무일수, H열~S열은 전월 근무 현황

T,U열은 9월1일(화), V,W열은 9월2일(수)
각열의 A는 오전 근무, P는 오후 근무, 옅은 하늘색은 휴무, 휴무일에 ㄱ은 추가근무, ㄱ)은 추가근무 취소

★ ★ ★ 
[역할]
당신은 엑셀 파워쿼리(Power Query) 전처리 전문가입니다. 
비정형 근무 배차표 엑셀을 DB 저장 및 데이터 분석이 가능한 세로형 정형 데이터(Tidy Data)로 변환하는 파워쿼리 M-Code 및 적용 가이드를 작성해 주세요.

[원본 데이터 구조 및 규칙]
1. 행/열 기본 구조:
   - 1행: 요일 (화, 수, 목...)
   - 2행: 일자 (9/1, 9/2...)
   - 3행: 일자별 총 근무 인원
   - A열: 지정휴무요일 | B열: 차번호 | C열: 기사명
   - D~Q열: 분석에 불필요하므로 완전히 제거(생략)
   - R열 이후: 날짜별 2개 열씩 짝을 이룸 (오전근무 / 오후근무)

2. 행별 지정 휴무 규칙 (배경색 대신 적용할 기준):
   - 3~6행: 일, 월 휴무
   - 7~8행: 월, 화 휴무
   - 9~12행: 화, 수 휴무
   - 13~14행: 수, 목 휴무
   - 15~18행: 목, 금 휴무
   - 19~20행: 금, 토 휴무
   - 21~28행: 토, 일 휴무

3. 근무 기호 의미:
   - A: 오전 근무 | P: 오후 근무
   - ㄱ: 휴무일 추가근무 | ㄱ): 추가근무 취소

[요청 사항]
1. 파워쿼리 작업 단계 가이드 (마우스 클릭 단계별 설명):
   - D~Q열 제거 단계
   - 1~3행 헤더 정리 및 날짜열(R열 이후) 피벗 해제(Unpivot) 단계
   - 2개씩 짝지어진 열을 '날짜'와 '근무시간대(오전/오후)'로 정형화하는 단계
   - 위 [행별 지정 휴무 규칙]과 [당일 요일]을 비교하여 '휴무 여부(Y/N)'를 판별하는 조건부 열 추가 단계

2. 고급 편집기용 전체 파워쿼리 M-Code 제공

[최종 결과 테이블 컬럼]
- 행번호 | 기사명 | 차량번호 | 지정휴무일 | 날짜 | 요일 | 근무시간대(오전/오후) | 근무코드 | 근무형태(정상근무/추가근무/추가근무취소) | 휴무여부(Y/N)



★ 제미나이가 짜준 프롬프트

```python
import os
import pandas as pd

files = os.listdir('.')
print(files)


```

```python
import pandas as pd

excel_file = "TEST.xlsx"
xls = pd.ExcelFile(excel_file)
print("Sheet names:", xls.sheet_names)
df = pd.read_excel(excel_file, sheet_name=0, nrows=10)
print(df.head(10))


```

비정형 엑셀 배차표/근무표 데이터를 데이터베이스(DB)나 통계 분석에 바로 활용할 수 있는 **일자별/기사별 정형화(Tidy Data) 데이터셋**으로 변환하기 위한 맞춤형 프롬프트입니다.

LLM(ChatGPT, Claude 등)에 전달할 때 아래 내용을 그대로 복사하여 사용하시면 됩니다.

---

**[프롬프트 복사 영역]**

> **[역할]**
> 당신은 엑셀 데이터 전처리 및 정형화(Data Tidying) 전문 데이터 엔지니어입니다. 비정형 엑셀 배차표를 relational DB나 데이터 분석에 최적화된 '행(Row) 기반 정형 데이터(Tidy Data)' 구조로 변환하는 작업을 수행합니다.
> **[원본 파일 구조 설명]**
> * **1행:** 요일 정보 (예: 화, 수...)
> * **2행:** 일자 정보 (예: 9월 1일, 9월 2일...)
> * **3행:** 해당 일자의 총 근무 인원 수
> * **A열:** 기사별 지정 휴무요일
> * **B열:** 차량 번호
> * **C열:** 기사 이름
> * **D열:** 만근 일수
> * **E~F열:** 감차 일수
> * **G열:** 실근무 일수
> * **H~S열:** 전월 근무 현황 데이터
> * **T열 이후 (날짜별 2개 열씩 짝을 이룸):**
> * 예: T, U열 = 9월 1일(화) / V, W열 = 9월 2일(수) ...
> * 첫 번째 열(예: T열): 오전 근무 구분 (`A`)
> * 두 번째 열(예: U열): 오후 근무 구분 (`P`)
> * Cell 배경색(옅은 하늘색): 지정 휴무일
> * 기호 `ㄱ`: 휴무일의 추가 근무
> * 기호 `ㄱ)`: 추가 근무 취소
> 
> 
> 
> 
> **[변환 및 정형화 규칙]**
> 1. **피벗 해제 (Unpivot / Melt):** 날짜별로 가로(열)로 늘어서 있는 데이터를 기사 1명당 날짜 1행 형태의 세로(행) 데이터로 변환하세요.
> 2. **시간대(오전/오후) 구분:** T/U, V/W 등 2개씩 짝지어진 열을 하나의 `날짜` 컬럼으로 통합하되, `근무시간대` 컬럼을 생성하여 '오전'과 '오후'를 구별하세요.
> 3. **근무 형태 판단 코드화:**
> * 값이 `A`인 경우 -> 근무시간대: 오전, 근무형태: 정상근무
> * 값이 `P`인 경우 -> 근무시간대: 오후, 근무형태: 정상근무
> * 배경색이 옅은 하늘색이거나 휴무 표시가 있는 경우 -> 휴무 여부: Y
> * 값이 `ㄱ`인 경우 -> 근무형태: 휴무일 추가근무
> * 값이 `ㄱ)`인 경우 -> 근무형태: 추가근무 취소
> 
> 
> 4. **요약 정보 분리:** D~G열(만근/감차/실근무일수) 및 H~S열(전월 현황)은 일자별 근무 기록과 섞이지 않도록 '기사 마스터 테이블' 또는 구분된 메타데이터 컬럼으로 분리하세요.
> 
> 
> **[최종 출력 데이터 컬럼 구조 (Target Schema)]**
> 생성 데이터셋은 다음 컬럼들을 가져야 합니다:
> * `기사명` (C열)
> * `차량번호` (B열)
> * `지정휴무요일` (A열)
> * `날짜` (YYYY-MM-DD 형식)
> * `요일` (1행 정보 기반)
> * `근무시간대` (오전 / 오후)
> * `근무코드` (A, P, ㄱ, ㄱ) 등 원본 기호)
> * `근무형태` (정상근무 / 휴무 / 추가근무 / 추가근무취소 등)
> * `휴무여부` (Y / N)
> 
> 
> **[요청 사항]**
> 위의 변환 규칙을 바탕으로:
> 1. 위 엑셀 파일을 정형화 데이터로 변환하기 위한 **Python (Pandas + openpyxl) 전처리 코드**를 작성해 주세요. (openpyxl을 활용하여 옅은 하늘색 셀 배경색 조건도 감지하도록 구현해 주세요.)
> 2. 정형화된 결과물의 예시 데이터 표(CSV / Table 형태 5행 내외)를 보여주세요.
> 
> 

---

**TIP:**
엑셀 파일 내의 '옅은 하늘색(셀 배경색)' 조건까지 LLM이 정확히 인지하려면 파이썬 코드(`openpyxl` 라이브러리)를 이용해 처리하는 것이 가장 확실합니다. 파이썬 코드 작성을 함께 요청해 두었으니 해당 코드를 실행하시거나 LLM에게 엑셀 파일과 함께 해당 프롬프트를 전달해 보세요!



★★ 1. 파워쿼리 적용 전 꼭 알아야 할 3가지 (소통 & 체크포인트)
셀 배경색(옅은 하늘색) 감지 문제

원인: 파워쿼리 기본 기능은 셀의 '값(텍스트)'만 읽어오며, 셀의 '배경색(하늘색)'을 직접 조건문으로 읽지 못합니다.

해결책:

방법 A (권장): 휴무일에 배경색만 칠하지 않고, 셀 안에 기호/텍스트(예: 휴, X, OFF 등)를 함께 기입하는 방식입니다.

방법 B (VBA/M코드 활용): 색상을 읽어내는 파워쿼리 Custom Function(M코드)이나 VBA를 병행하여 '색상 코드' 열을 먼저 만들어놓고 시작하는 방식입니다.

D~G열(만근/감차/실근무) 및 H~S열(전월 현황) 처리 방향

이 열들은 '일자별 근무 내역'이 아니라 기사별 '월간 합계/메타데이터'입니다.

일자별로 데이터를 세로로 길게 피벗 해제(Unpivot)할 때 이 열들이 매 행마다 반복 표시되게 둘 것인지, 아니면 아예 파워쿼리 안에서 [기사 정보 테이블]과 [일자별 근무 테이블] 2개로 분리할지 결정이 필요합니다.

날짜 열 2개 짝짓기 (T/U, V/W...)

1개 일자당 2개의 열(오전/오후)로 나뉘어 있으므로, 파워쿼리에서 '행 피벗 해제(Unpivot)' -> '짝수/홀수 열 분리 or 조건부 열 추가' 과정을 통해 하나의 날짜로 묶는 단계가 들어갑니다.

2. 파워쿼리(Power Query) 정형화 전용 프롬프트
아래 프롬프트를 LLM(ChatGPT, Claude 등)에 전달하시면 파워쿼리 M코드 및 단계별 클릭 가이드를 받으실 수 있습니다.

Plaintext
[역할]
당신은 엑셀 파워쿼리(Power Query / M code) 데이터 전처리 전문가입니다. 
비정형 엑셀 근무 배차표를 엑셀 파워쿼리를 사용하여 DB/통계 분석에 적합한 세로형 정형 데이터(Tidy Data)로 변환하는 단계별 가이드와 M-Code를 작성해 주세요.

[원본 데이터 구조]
- 1행: 요일 (화, 수, 목...)
- 2행: 일자 (9월 1일, 9월 2일...)
- 3행: 당일 총 근무 인원수
- A열: 휴무요일 | B열: 차번호 | C열: 기사명 | D~G열: 만근/감차/실근무일수 | H~S열: 전월근무현황
- T열 이후: 날짜별 2열씩 짝을 이룸
  * T열: 9/1(화) 오전 근무(A) / U열: 9/1(화) 오후 근무(P)
  * V열: 9/2(수) 오전 근무(A) / W열: 9/2(수) 오후 근무(P)
  * 기호 'ㄱ': 휴무일 추가근무 / 'ㄱ)': 추가근무 취소
  * (주의: 셀 배경색이 하늘색인 경우 지정 휴무일임)

[변환 목표 및 요청사항]
1. 셀 배경색 한계 극복 대안 제시:
   - 파워쿼리에서 배경색을 직접 인식하지 못하는 점을 고려하여, 파워쿼리 편집기 내에서 '지정휴무요일(A열)'과 '당일 요일'을 비교하여 휴무 여부를 판별하는 조건부 열 생성 로직을 포함해 주세요.

2. 파워쿼리 단계별 작업 가이드 (GUI 마우스 클릭 순서):
   - 1~3행 헤더 처리 (머지 및 행 제거 방법)
   - A~C열(기사 정보)과 D~S열(요약 정보) 유지 후, 날짜열(T열 이후) 피벗 해제(Unpivot) 단계
   - 2개씩 짝지어진 열(오전/오후)을 하나의 '날짜'와 '근무시간대(오전/오후)'로 정형화하는 단계

3. 파워쿼리 M-Code 제공:
   - 전체 변환 과정을 한 번에 적용할 수 있는 [고급 편집기]용 M-Code 전체 구조를 작성해 주세요.

[최종 결과 테이블 스키마]
- 기사명 | 차량번호 | 지정휴무요일 | 날짜 | 요일 | 근무시간대(오전/오후) | 근무코드(A/P/ㄱ/ㄱ)) | 근무형태(정상근무/추가근무/추가근무취소) | 휴무여부(Y/N)
언제든 위 3가지 포인트 중 추가로 정하고 싶으신 부분(예: "배경색 대신 셀에 '휴'라고 입력하겠다" 또는 "파워쿼리 안에서 테이블을 2개로 나누고 싶다")이 있다면 편하게 말씀해 주세요!
